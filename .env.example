# Copy to .env for local runs of scripts/bakeoff.py. .env is gitignored.
# Only the model keys belong here. The daily pipeline runs in Actions and reads
# its Gmail credentials from repository secrets, so keeping them out of this file
# means a local run cannot mail anyone by accident.

GEMINI_API_KEY=
GROQ_API_KEY=
